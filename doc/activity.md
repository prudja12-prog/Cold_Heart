@startuml
skinparam conditionStyle inside
title Алгоритм системы "Холодное сердце" (Управление климатом)

start

:Эльза: создатьБурю();

if (Эмоциональный сбой?) then (да)
    :Активирована снежная буря;

    :Эльза: заморозить замок;

    if (Создать Олафа?) then (да)
        :Эльза: создатьОлафа();
    else (нет)
        :Продолжить без Олафа;
    endif

else (нет)
    :Система работает нормально (лето);
endif

' --- Этап 1: реакция жителей
if (Жители страдают от холода?) then (да)
    :Жители пытаются выжить;
else (нет)
    :Жизнь продолжается нормально;
endif

' --- Этап 2: попытка Анны
if (Анна активирует любовь?) then (да)
    if (Сила любви >= сила магии?) then (да)
        :Зима остановлена; <<#lightgreen>>
        :Система: растопить зиму;
    else (нет)
        :Зима продолжается;
    endif
else (нет)
    :Никто не пытается остановить зиму;
endif

' --- Внешнее прерывание
partition "Критическая ситуация" {
    :Система перегружена холодом;

    if (Температура критическая?) then (да)
        :Полная заморозка королевства; <<#pink>>
    else (нет)
        :Система удерживает стабильность;
    endif
}

:Система стабилизирована (лето); <<#lightgreen>>
stop

@enduml